# Pip alternative to environment.yml. From inside this folder, in a Python 3.11+ env:
#     pip install -r requirements.txt
#     jupyter lab thesis_pipeline.ipynb
# Pinned to the environment the results were produced in.
numpy==2.3.5
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.7.2
matplotlib==3.10.6
networkx==3.5
jupyterlab==4.4.7
autodp==0.2.3.1
# MST mechanism backend (JAX), pinned:
jax==0.10.0
jaxlib==0.10.0
chex==0.1.91
optax==0.2.8
attrs==25.4.0
# MST library bundled in this repo (installs the `mbi` package):
./private-pgm
